# Serveur temps réel
fastapi>=0.115
uvicorn[standard]>=0.32
websockets>=13.0
pydantic-settings>=2.5
python-multipart>=0.0.12   # parsing des webhooks Twilio (form-encoded)

# Téléphonie
twilio>=9.3

# Base de données (Postgres/Supabase — optionnelle en dev)
asyncpg>=0.30

# Boucle conversationnelle
anthropic>=0.40
httpx>=0.27          # client TTS ElevenLabs streaming

# Dev / tests
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.7
